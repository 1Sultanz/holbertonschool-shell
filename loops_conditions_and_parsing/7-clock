#!/usr/bin/env bash
# Clock: Sənin strukturla, istənilən formatda

b=0    # hour

while [ $b -le 12 ]
do
	echo "Hour: $b"
	a=1
  while [ $a -le 59 ]
  do
	  echo "$a"
	  ((a++))
  done
  ((b++))
done | head -n 70
